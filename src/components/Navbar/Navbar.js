import React from "react";
import "./Navbar.css";

const Navbar = props => (
    <div>
        <div class="topnav">
            <a class="active" href="#home">Clicky Game || </a>
            <a href="#news">News</a>
        </div>
    </div>
);

export default Navbar;